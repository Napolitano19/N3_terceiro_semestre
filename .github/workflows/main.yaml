name: Node.js CI

on:
  push:
    branches:
      - testeworkflow
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # Configuração do Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20'  # Substitua pela versão correta do Node.js

      # Executar testes com Jest
      - name: Executar testes com Jest
        working-directory: back_end
        run: npm test
         